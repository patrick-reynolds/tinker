
<% if user_signed_in? %>
<li id="fat-menu" class="dropdown">
	<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    Account <b class="caret"></b> </a>  
         <ul class="dropdown-menu">
               <li><%= link_to("PayPal", '#') %></li>
               <li><%= link_to("Profile", edit_user_registration_path) %></li>
               <li class="divider"></li>
               <li><%= link_to("Sign out", destroy_user_session_path, method: "delete") %></li>
         </ul>
</li>

<% else %>
	<% if band_signed_in? %>
		<li id="fat-menu" class="dropdown">
		<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    	Account <b class="caret"></b> </a>
	         <ul class="dropdown-menu">
	               <li><%= link_to("Band Page", current_band) %></li>
	               <li><%= link_to("Profile", edit_band_registration_path) %></li>
               		<li><%= link_to("PayPal", '#') %></li>
	               <li class="divider"></li>
	               <li><%= link_to "Sign out", destroy_band_session_path, method: "delete" %></li>
	         </ul>
		</li>
	<% else %>
		<li id="fat-menu" class="dropdown">
		<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    	Sign up <b class="caret"></b> </a>
	         <ul class="dropdown-menu">
	               <li><%= link_to("User Sign up", new_user_registration_path) %></li>
	               <li><%= link_to("Band Sign up", new_band_registration_path) %></li>
	         </ul>
		</li>
		<li id="fat-menu" class="dropdown">
		<a href="#" class="dropdown-toggle" data-toggle="dropdown">
    	Login <b class="caret"></b> </a>
	         <ul class="dropdown-menu">
	               <li><%= link_to("User Login", new_user_session_path) %></li>
	               <li><%= link_to("Band Login", new_band_session_path) %></li>
	         </ul>
		</li>
	<% end %> 
<% end %> 

